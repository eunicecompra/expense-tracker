dist: trusty
sudo: required

addons:
  apt:
    packages:
      - google-chrome-stable

before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start &
  - sleep 3

language: node_js
node_js:
  - '6.11.0'

before_script:
  - 'npm install --only=dev'


